import random

# Generate a random value between 100 and 500 for simulation
random_value = random.randint(100, 500)

# Print the value in the specified format
print(f"Weight of the item = {random_value} grams")
